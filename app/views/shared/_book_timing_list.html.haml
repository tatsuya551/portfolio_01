.book-list.box-center.main-font
  - if impressions.present?
    .book-list__main.box-list
      - impressions.each do |impression|
        .book-list__main__box.text-center.book-hover
          = link_to book_impressions_path(impression.book), class: "book-list__main__box__content text-line-none text-black" do
            - if impression.book.category == "novel"
              .book-category-tag.background-novel
                小説
            - elsif impression.book.category == "management"
              .book-category-tag.background-management
                経営
            - elsif impression.book.category == "economy"
              .book-category-tag.background-economy
                経済
            - elsif impression.book.category == "finance"
              .book-category-tag.background-finance
                金融
            - elsif impression.book.category == "it"
              .book-category-tag.background-it
                IT
            - elsif impression.book.category == "motivation"
              .book-category-tag.background-motivation
                自己啓発
            - elsif impression.book.category == "talent"
              .book-category-tag.background-talent
                タレント
            - else
              .book-category-tag.background-etc
                その他
            .book-status
              .book-status__title.font-bold.text-white
                読書済
            .book-shadow
            .book-image
              = image_tag impression.book.image.to_s, class: "image-set"
            .book-title
              = impression.book.title
  - else
    .book-list__none.text-center
      登録された本がありません