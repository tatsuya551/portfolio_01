.mypage-book-impression-list.box-center.main-font
  - if impressions.present?
    .mypage-book-impression-list__main.box-list
      - impressions.each do |impression|
        .mypage-book-impression-list__main__box.text-center.book-hover
          = link_to book_impressions_path(impression.book, impression), class: "mypage-book-impression-list__main__box__content text-line-none text-black " do
            - if impression.book.category == "小説"
              .book-category-tag.background-novel
                小説
            - elsif impression.book.category == "経営・戦略"
              .book-category-tag.background-management
                経営
            - elsif impression.book.category == "政治・経済"
              .book-category-tag.background-economy
                経済
            - elsif impression.book.category == "金融・ファイナンス"
              .book-category-tag.background-finance
                金融
            - elsif impression.book.category == "IT"
              .book-category-tag.background-it
                IT
            - elsif impression.book.category == "自己啓発"
              .book-category-tag.background-motivation
                自己啓発
            - elsif impression.book.category == "タレント本"
              .book-category-tag.background-talent
                タレント
            - else
              .book-category-tag.background-etc
                その他
            .book-status
              .book-status__title.font-bold.text-white
                読書済
            .book-shadow
            .book-image
              = image_tag impression.book.image.to_s, class: "image-set"
            .book-title
              = impression.book.title
  - else
    .mypage-book-impression-list__none.text-list
      登録された本がありません
